dist
node_modules
reference
package-lock.json
# Next.js regenerates this triple-slash-reference file on every build; never hand-edited.
apps/web/next-env.d.ts

# Specification and project docs are not reformatted — they are the authoritative
# source of truth (CLAUDE.md) and hand-authored; leave their prose untouched.
docs
CLAUDE.md
README.md

# Recorded marketplace fixtures are kept byte-for-byte as the source delivered them; a
# reformat would reflow the embedded page payload the parser is tested against.
packages/adapters/src/trendyol/fixtures/public-page.html
packages/adapters/src/hepsiburada/fixtures/public-listings-BS1372.json
