/**
 * apps/web — Next.js 16 UI, API routes, setup wizard (docs/06, doc 10 §1.1, §6).
 * Empty on purpose: Phase 6 has not started yet. This file exists so the app builds
 * as part of the Phase 1 monorepo scaffold. The real Next.js app shell is 6.1.
 */
export {};
