/**
 * apps/worker — long-running job host: scheduler, jobs, rate limiting, budget, outbox
 * (doc 07, doc 10 §1). Empty on purpose: Phase 5 has not started yet. This file exists
 * so the app builds as part of the Phase 1 monorepo scaffold.
 */
export {};
